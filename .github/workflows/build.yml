name: BuildJet Kernel

on:
  workflow_dispatch:

env:
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

jobs:
  build:
    name: Build Surya Kernel
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Kernel Source
        uses: actions/checkout@v3

      - name: Prepare timezone
        run: |
          echo "BUILD_TIME=$(TZ=Asia/Jakarta date '+%d%m%Y-%H%M')" >> $GITHUB_ENV
          sudo ln -sf /usr/share/zoneinfo/Asia/Jakarta /etc/localtime

      - name: Install Dependencies
        run: |
          sudo apt update -y
          sudo apt install -y bc cpio flex bison aptitude git python-is-python3 tar perl wget curl lz4 device-tree-compiler libssl-dev

      - name: Clone ZyC Clang
        run: |
          mkdir -p zyc-clang
          curl -L -o Clang.tar.gz https://github.com/ZyCromerZ/Clang/releases/download/20.0.0git-20250129-release/Clang-20.0.0git-20250129.tar.gz
          tar -xf Clang.tar.gz -C zyc-clang --strip-components=1

      - name: Set Environment Variables
        run: |
          echo "KERNEL_DEFCONFIG=surya_defconfig" >> $GITHUB_ENV
          echo "TOOLCHAIN=$(pwd)/zyc-clang" >> $GITHUB_ENV
          echo "PATH=$(pwd)/zyc-clang/bin:$PATH" >> $GITHUB_ENV
          echo "BUILD_USER=mahirooo" >> $GITHUB_ENV
          echo "BUILD_HOST=android-build" >> $GITHUB_ENV
          echo "BUILD_TIME=$(date +%Y%m%d-%H%M)" >> $GITHUB_ENV

      - name: Send Telegram Notification - Build Started
        run: |
          CLANG_VER=$($(pwd)/zyc-clang/bin/clang --version | head -n 1)
          curl -s -X POST https://api.telegram.org/bot${{ env.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ env.TELEGRAM_CHAT_ID }} \
            -d text="🚀 *Build Dimulai!* | *Clang*: ${CLANG_VER}" \
            -d parse_mode=Markdown

      - name: Set Build Start Time
        run: echo "BUILD_START=$(date +%s)" >> $GITHUB_ENV

      - name: Clean Build Output
        run: |
          make O=out ARCH=arm64 clean
          make O=out ARCH=arm64 mrproper

      - name: Make Defconfig
        run: |
          make O=out ARCH=arm64 ${KERNEL_DEFCONFIG}

      - name: Build Kernel
        run: |
          make -j$(nproc) O=out \
            ARCH=arm64 \
            LD=ld.lld \
            AS=llvm-as \
            AR=llvm-ar \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            STRIP=llvm-strip \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_COMPAT=arm-linux-gnueabi- \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            LLVM=1 \
            LLVM_IAS=1 \
            DTC_EXT=dtc \
            KBUILD_COMPILER_STRING="$(pwd)/zyc-clang" \
            KBUILD_BUILD_USER=${BUILD_USER} \
            KBUILD_BUILD_HOST=${BUILD_HOST} 2>&1 | tee log.txt
        continue-on-error: true

      - name: Check Kernel Output Files
        run: |
          if [[ ! -f out/arch/arm64/boot/Image.gz ]] || [[ ! -f out/arch/arm64/boot/dtbo.img ]] || [[ ! -f out/arch/arm64/boot/dtb.img ]]; then
            echo "❌ Kernel output files missing."
            exit 1
          fi

      - name: Send Telegram Notification - Build Failed (if failed)
        if: failure()
        run: |
          if [ -f log.txt ]; then
            curl -s -X POST https://api.telegram.org/bot${{ env.TELEGRAM_BOT_TOKEN }}/sendDocument \
              -F chat_id=${{ env.TELEGRAM_CHAT_ID }} \
              -F document=@log.txt \
              -F caption="❌ Build kernel *gagal*! Berikut log-nya 🧾." \
              -F parse_mode=Markdown
          else
            echo "❌ log.txt not found!"
          fi

      - name: Fetch AnyKernel3 and Package
        if: success()
        run: |
          git clone --depth=1 https://github.com/rinnsakaguchi/AnyKernel3.git -b FSociety AnyKernel3
          cp out/arch/arm64/boot/Image.gz AnyKernel3/Image.gz
          cp out/arch/arm64/boot/dtbo.img AnyKernel3/dtbo.img
          cp out/arch/arm64/boot/dtb.img AnyKernel3/dtb.img
          cd AnyKernel3
          zip -r9 ../Surya-Kernel-${{ env.BUILD_TIME }}.zip ./*

      - name: Send Build Success to Telegram
        if: success()
        run: |
          curl -F document=@Surya-Kernel-${{ env.BUILD_TIME }}.zip \
            -F caption="✅ Build Success with ZyC-Clang" \
            -F parse_mode=Markdown \
            "https://api.telegram.org/bot${{ env.TELEGRAM_BOT_TOKEN }}/sendDocument?chat_id=${{ env.TELEGRAM_CHAT_ID }}"
